import 'package:flutter/material.dart';

class WrdMid extends StatelessWidget {
  const WrdMid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(
      children: const [
        Text("wrdMid"),
      ],
    );
  }
}